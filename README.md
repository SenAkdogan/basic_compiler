# basic_compiler
# ESALang - A Simple Interpreter

ESALang is a custom-built interpreter written in Python. It supports arithmetic operations, variable assignments, string manipulation, and control flow logic.

## 🚀 Features

- **Arithmetic Operations:** Addition, Subtraction, Multiplication, Division.
- **Operator Precedence:** Correctly handles order of operations and parentheses `( )`.
- **Variables:** Store and retrieve values using dynamic typing (Integers and Strings).
- **String Operations:** Concatenation (`+`) and Repetition (`*`).
- **Control Flow:** `if-then-else` conditional logic.
- **Comparisons:** Supports `==`, `!=`, `>`, `<`.

## 🛠️ Installation & Usage

### Option 1: Run with Python
Ensure you have Python installed.
```bash
python compiler.py
